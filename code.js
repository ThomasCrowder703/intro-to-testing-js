// helloWorld function 
const helloWorld = function() {
    return "Hello, World!";
}

const sayHello = function (input) {
      return "Hello " + input + "!"
}
//This should work because any data type entered will be concatenated into a string.
